***For initial or fresh commit ***
    - pwd
    -git init
    -git status
    -(create floders and file initial)
    -git add.
    -git commit -m "massage like added day1"

***GitHub part ***
    -(go to github and create your repository and copy repository url)
    -git remote add origin https://github.com/repopath/parctice.git
    -git branch -M main
    -git push -u origin main


***next day commit***
    -(write your code in a folder let's say Day12)
    -git status
    -(check what changes )
    -git add .
    -git commit -m "added day 12 "
    -git push
